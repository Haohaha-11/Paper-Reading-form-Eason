[← 返回 README](../README.md)

# Abstract

## 📌 预览
摘要给出全文主线：VLM 的 connector 不只是维度投影，而是在决定视觉 token 是否落入 LLM 能理解的 latent 区域。AlignVLM 的核心做法是把每个视觉特征表示成 LLM text embeddings 的加权平均。

---

> 💡 **Q&A 批注记录**:
> - Q: 为什么 connector 是这篇的中心，而不是 vision encoder 或 LLM？
> - A: 文档 VLM 通常已经有强 vision encoder 和 LLM，瓶颈在视觉 patch features 如何变成 LLM input embeddings。若 connector 产生 OOD latent，后面的 LLM 再强也难以稳定解释。

# ALIGNVLM: Bridging Vision and Language Latent Spaces for Multimodal Document Understanding

Ahmed Masry1,2, Juan A. Rodriguez1,3,4, Tianyu Zhang1,3,5, Suyuchen Wang1,3,5, Chao Wang1, Aarash Feizi1,3,6, Akshay Kalkunte Suresh1, Abhay Puri1, Xiangru Jian1,7, Pierre-André Noël1, Sathwik Tejaswi Madhusudhan1, Marco Pedersoli1,4, Bang Liu1,5,8, Nicolas Chapados1, Yoshua Bengio3,5,8,   
Enamul Hoque2, Christopher $\mathbf { P a l } ^ { 1 , 3 , 8 , 9 }$ , Issam H. Laradji1,10, David Vazquez1, Perouz Taslakian1,3,6, Spandana Gella1, Sai Rajeswar1,3,5
> 💡 **作者/机构读法**: ServiceNow + Mila/Université de Montréal/Yoshua Bengio 等合作，论文强调 license-permissive 数据和商业文档场景；这解释了它为什么关注 document understanding、透明数据和高效 connector。

1ServiceNow 2York University 3Mila – Quebec AI Institute   
4École de Technologie Supérieure 5Université de Montréal   
6McGill University 7University of Waterloo 8CIFAR AI Chair   
9Polytechnique Montréal 10University of British Columbia
> 💡 **读法提示**: 摘要里最重要的四个短语是 connector、weighted average of LLM text embeddings、document understanding、low-resource/noise robustness。后文所有实验都围绕这四点展开。

# Abstract

Aligning visual features with language embeddings is a key challenge in visionlanguage models (VLMs). The performance of such models hinges on having a good connector that maps visual features generated by a vision encoder to a shared embedding space with the LLM while preserving semantic similarity. Existing connectors, such as multilayer perceptrons (MLPs), lack inductive bias to constrain visual features within the linguistic structure of the LLM’s embedding space, making them data-hungry and prone to cross-modal misalignment. In this work, we propose a novel vision-text alignment method, ALIGNVLM, that maps visual features to a weighted average of LLM text embeddings. Our approach leverages the linguistic priors encoded by the LLM to ensure that visual features are mapped to regions of the space that the LLM can effectively interpret. ALIGNVLM is particularly effective for document understanding tasks, where visual and textual modalities are highly correlated. Our extensive experiments show that ALIGNVLM achieves state-of-the-art performance compared to prior alignment methods, with larger gains on document understanding tasks and under low-resource setups. We provide further analysis demonstrating its efficiency and robustness to noise.

> 💡 **摘要主线**: 摘要的因果链是：MLP connector 缺少 text-space 约束，视觉特征可能进入 LLM 未熟悉的 latent 区域；ALIGN 用词表概率加权 text embeddings，把视觉 token 拉回 LLM 可解释空间。这里的贡献不是新 LLM，而是一个带语言先验的 shallow-fusion connector。

---

## 🔖 Section 总结

### 关键数字速查
| 指标 | 数值 |
|------|------|
| 方法一句话 | visual features → distribution over LLM vocabulary → weighted average of text embeddings |
| 适用场景 | multimodal document understanding |
| 主要 claim | SOTA/competitive performance、低资源收益、噪声鲁棒、效率开销小 |

### 核心洞察
1. AlignVLM 的“alignment”不是对比学习式全局图文对齐，而是 LLM input embedding 层的局部连接器设计。
2. 加权平均 text embeddings 等价于给视觉 token 一个语言坐标系，这对文档图像里的文字、数字、表格尤其自然。
3. 读后文要检查三个证据：同训练设置下 connector 对照是否赢、低资源收益是否更大、噪声扰动是否显示约束有效。

### 可追问点
- 视觉 patch 映射到词表概率会不会损失连续视觉细节？
- 为什么 text embedding convex hull 能降低 OOD latent？
- 这种方法能否迁移到医学影像这类非文本视觉域？
